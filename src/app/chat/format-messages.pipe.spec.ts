import { FormatMessagesPipe } from './format-messages.pipe';

describe('FormatMessagesPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatMessagesPipe();
    expect(pipe).toBeTruthy();
  });
});
